# json 설명

 - `file_version`: 파일 표준 버전입니다. 현재 버전은 1입니다.
   - 파일 유효성 체크 등에 사용됩니다.
 - `data`: `user_id`와 `xp`를 한 쌍으로 묶어서 유저들의 경험치 값을 저장해두는 곳입니다.
   - 봇에 따라서는 `xp`의 값이 음수일 수 있습니다. 음수 값을 지원하지 않는 봇에서 json 파일을 가져올 때는 음수 값을 0으로 처리하는 등 적절한 예외 처리를 하시기 바랍니다. (가급적이면 음수 값을 지원하도록 개발하는 것을 권장함)
